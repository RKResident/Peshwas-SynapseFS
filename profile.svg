<?xml version="1.0" standalone="no"?><!DOCTYPE svg PUBLIC "-//W3C//DTD SVG 1.1//EN" "http://www.w3.org/Graphics/SVG/1.1/DTD/svg11.dtd"><svg version="1.1" width="1200" height="362" onload="init(evt)" viewBox="0 0 1200 362" xmlns="http://www.w3.org/2000/svg" xmlns:xlink="http://www.w3.org/1999/xlink" xmlns:fg="http://github.com/jonhoo/inferno"><!--Flame graph stack visualization. See https://github.com/brendangregg/FlameGraph for latest version, and http://www.brendangregg.com/flamegraphs.html for examples.--><!--NOTES: --><defs><linearGradient id="background" y1="0" y2="1" x1="0" x2="0"><stop stop-color="#eeeeee" offset="5%"/><stop stop-color="#eeeeb0" offset="95%"/></linearGradient></defs><style type="text/css">
text { font-family:monospace; font-size:12px }
#title { text-anchor:middle; font-size:17px; }
#matched { text-anchor:end; }
#search { text-anchor:end; opacity:0.1; cursor:pointer; }
#search:hover, #search.show { opacity:1; }
#subtitle { text-anchor:middle; font-color:rgb(160,160,160); }
#unzoom { cursor:pointer; }
#frames > *:hover { stroke:black; stroke-width:0.5; cursor:pointer; }
.hide { display:none; }
.parent { opacity:0.5; }
</style><script type="text/ecmascript"><![CDATA[
        var nametype = 'Function:';
        var fontsize = 12;
        var fontwidth = 0.59;
        var xpad = 10;
        var inverted = true;
        var searchcolor = 'rgb(230,0,230)';
        var fluiddrawing = true;
        var truncate_text_right = false;
    ]]><![CDATA["use strict";
var details, searchbtn, unzoombtn, matchedtxt, svg, searching, frames, known_font_width;
function init(evt) {
    details = document.getElementById("details").firstChild;
    searchbtn = document.getElementById("search");
    unzoombtn = document.getElementById("unzoom");
    matchedtxt = document.getElementById("matched");
    svg = document.getElementsByTagName("svg")[0];
    frames = document.getElementById("frames");
    known_font_width = get_monospace_width(frames);
    total_samples = parseInt(frames.attributes.total_samples.value);
    searching = 0;

    // Use GET parameters to restore a flamegraph's state.
    var restore_state = function() {
        var params = get_params();
        if (params.x && params.y)
            zoom(find_group(document.querySelector('[*|x="' + params.x + '"][y="' + params.y + '"]')));
        if (params.s)
            search(params.s);
    };

    if (fluiddrawing) {
        // Make width dynamic so the SVG fits its parent's width.
        svg.removeAttribute("width");
        // Edge requires us to have a viewBox that gets updated with size changes.
        var isEdge = /Edge\/\d./i.test(navigator.userAgent);
        if (!isEdge) {
            svg.removeAttribute("viewBox");
        }
        var update_for_width_change = function() {
            if (isEdge) {
                svg.attributes.viewBox.value = "0 0 " + svg.width.baseVal.value + " " + svg.height.baseVal.value;
            }

            // Keep consistent padding on left and right of frames container.
            frames.attributes.width.value = svg.width.baseVal.value - xpad * 2;

            // Text truncation needs to be adjusted for the current width.
            update_text_for_elements(frames.children);

            // Keep search elements at a fixed distance from right edge.
            var svgWidth = svg.width.baseVal.value;
            searchbtn.attributes.x.value = svgWidth - xpad;
            matchedtxt.attributes.x.value = svgWidth - xpad;
        };
        window.addEventListener('resize', function() {
            update_for_width_change();
        });
        // This needs to be done asynchronously for Safari to work.
        setTimeout(function() {
            unzoom();
            update_for_width_change();
            restore_state();
        }, 0);
    } else {
        restore_state();
    }
}
// event listeners
window.addEventListener("click", function(e) {
    var target = find_group(e.target);
    if (target) {
        if (target.nodeName == "a") {
            if (e.ctrlKey === false) return;
            e.preventDefault();
        }
        if (target.classList.contains("parent")) unzoom();
        zoom(target);

        // set parameters for zoom state
        var el = target.querySelector("rect");
        if (el && el.attributes && el.attributes.y && el.attributes["fg:x"]) {
            var params = get_params()
            params.x = el.attributes["fg:x"].value;
            params.y = el.attributes.y.value;
            history.replaceState(null, null, parse_params(params));
        }
    }
    else if (e.target.id == "unzoom") {
        unzoom();

        // remove zoom state
        var params = get_params();
        if (params.x) delete params.x;
        if (params.y) delete params.y;
        history.replaceState(null, null, parse_params(params));
    }
    else if (e.target.id == "search") search_prompt();
}, false)
// mouse-over for info
// show
window.addEventListener("mouseover", function(e) {
    var target = find_group(e.target);
    if (target) details.nodeValue = nametype + " " + g_to_text(target);
}, false)
// clear
window.addEventListener("mouseout", function(e) {
    var target = find_group(e.target);
    if (target) details.nodeValue = ' ';
}, false)
// ctrl-F for search
window.addEventListener("keydown",function (e) {
    if (e.keyCode === 114 || (e.ctrlKey && e.keyCode === 70)) {
        e.preventDefault();
        search_prompt();
    }
}, false)
// functions
function get_params() {
    var params = {};
    var paramsarr = window.location.search.substr(1).split('&');
    for (var i = 0; i < paramsarr.length; ++i) {
        var tmp = paramsarr[i].split("=");
        if (!tmp[0] || !tmp[1]) continue;
        params[tmp[0]]  = decodeURIComponent(tmp[1]);
    }
    return params;
}
function parse_params(params) {
    var uri = "?";
    for (var key in params) {
        uri += key + '=' + encodeURIComponent(params[key]) + '&';
    }
    if (uri.slice(-1) == "&")
        uri = uri.substring(0, uri.length - 1);
    if (uri == '?')
        uri = window.location.href.split('?')[0];
    return uri;
}
function find_child(node, selector) {
    var children = node.querySelectorAll(selector);
    if (children.length) return children[0];
    return;
}
function find_group(node) {
    var parent = node.parentElement;
    if (!parent) return;
    if (parent.id == "frames") return node;
    return find_group(parent);
}
function orig_save(e, attr, val) {
    if (e.attributes["fg:orig_" + attr] != undefined) return;
    if (e.attributes[attr] == undefined) return;
    if (val == undefined) val = e.attributes[attr].value;
    e.setAttribute("fg:orig_" + attr, val);
}
function orig_load(e, attr) {
    if (e.attributes["fg:orig_"+attr] == undefined) return;
    e.attributes[attr].value = e.attributes["fg:orig_" + attr].value;
    e.removeAttribute("fg:orig_" + attr);
}
function g_to_text(e) {
    var text = find_child(e, "title").firstChild.nodeValue;
    return (text)
}
function g_to_func(e) {
    var func = g_to_text(e);
    // if there's any manipulation we want to do to the function
    // name before it's searched, do it here before returning.
    return (func);
}
function get_monospace_width(frames) {
    // Given the id="frames" element, return the width of text characters if
    // this is a monospace font, otherwise return 0.
    text = find_child(frames.children[0], "text");
    originalContent = text.textContent;
    text.textContent = "!";
    bangWidth = text.getComputedTextLength();
    text.textContent = "W";
    wWidth = text.getComputedTextLength();
    text.textContent = originalContent;
    if (bangWidth === wWidth) {
        return bangWidth;
    } else {
        return 0;
    }
}
function update_text_for_elements(elements) {
    // In order to render quickly in the browser, you want to do one pass of
    // reading attributes, and one pass of mutating attributes. See
    // https://web.dev/avoid-large-complex-layouts-and-layout-thrashing/ for details.

    // Fall back to inefficient calculation, if we're variable-width font.
    // TODO This should be optimized somehow too.
    if (known_font_width === 0) {
        for (var i = 0; i < elements.length; i++) {
            update_text(elements[i]);
        }
        return;
    }

    var textElemNewAttributes = [];
    for (var i = 0; i < elements.length; i++) {
        var e = elements[i];
        var r = find_child(e, "rect");
        var t = find_child(e, "text");
        var w = parseFloat(r.attributes.width.value) * frames.attributes.width.value / 100 - 3;
        var txt = find_child(e, "title").textContent.replace(/\([^(]*\)$/,"");
        var newX = format_percent((parseFloat(r.attributes.x.value) + (100 * 3 / frames.attributes.width.value)));

        // Smaller than this size won't fit anything
        if (w < 2 * known_font_width) {
            textElemNewAttributes.push([newX, ""]);
            continue;
        }

        // Fit in full text width
        if (txt.length * known_font_width < w) {
            textElemNewAttributes.push([newX, txt]);
            continue;
        }

        var substringLength = Math.floor(w / known_font_width) - 2;
        if (truncate_text_right) {
            // Truncate the right side of the text.
            textElemNewAttributes.push([newX, txt.substring(0, substringLength) + ".."]);
            continue;
        } else {
            // Truncate the left side of the text.
            textElemNewAttributes.push([newX, ".." + txt.substring(txt.length - substringLength, txt.length)]);
            continue;
        }
    }

    console.assert(textElemNewAttributes.length === elements.length, "Resize failed, please file a bug at https://github.com/jonhoo/inferno/");

    // Now that we know new textContent, set it all in one go so we don't refresh a bazillion times.
    for (var i = 0; i < elements.length; i++) {
        var e = elements[i];
        var values = textElemNewAttributes[i];
        var t = find_child(e, "text");
        t.attributes.x.value = values[0];
        t.textContent = values[1];
    }
}

function update_text(e) {
    var r = find_child(e, "rect");
    var t = find_child(e, "text");
    var w = parseFloat(r.attributes.width.value) * frames.attributes.width.value / 100 - 3;
    var txt = find_child(e, "title").textContent.replace(/\([^(]*\)$/,"");
    t.attributes.x.value = format_percent((parseFloat(r.attributes.x.value) + (100 * 3 / frames.attributes.width.value)));

    // Smaller than this size won't fit anything
    if (w < 2 * fontsize * fontwidth) {
        t.textContent = "";
        return;
    }
    t.textContent = txt;
    // Fit in full text width
    if (t.getComputedTextLength() < w)
        return;
    if (truncate_text_right) {
        // Truncate the right side of the text.
        for (var x = txt.length - 2; x > 0; x--) {
            if (t.getSubStringLength(0, x + 2) <= w) {
                t.textContent = txt.substring(0, x) + "..";
                return;
            }
        }
    } else {
        // Truncate the left side of the text.
        for (var x = 2; x < txt.length; x++) {
            if (t.getSubStringLength(x - 2, txt.length) <= w) {
                t.textContent = ".." + txt.substring(x, txt.length);
                return;
            }
        }
    }
    t.textContent = "";
}
// zoom
function zoom_reset(e) {
    if (e.tagName == "rect") {
        e.attributes.x.value = format_percent(100 * parseInt(e.attributes["fg:x"].value) / total_samples);
        e.attributes.width.value = format_percent(100 * parseInt(e.attributes["fg:w"].value) / total_samples);
    }
    if (e.childNodes == undefined) return;
    for(var i = 0, c = e.childNodes; i < c.length; i++) {
        zoom_reset(c[i]);
    }
}
function zoom_child(e, x, zoomed_width_samples) {
    if (e.tagName == "text") {
        var parent_x = parseFloat(find_child(e.parentNode, "rect[x]").attributes.x.value);
        e.attributes.x.value = format_percent(parent_x + (100 * 3 / frames.attributes.width.value));
    } else if (e.tagName == "rect") {
        e.attributes.x.value = format_percent(100 * (parseInt(e.attributes["fg:x"].value) - x) / zoomed_width_samples);
        e.attributes.width.value = format_percent(100 * parseInt(e.attributes["fg:w"].value) / zoomed_width_samples);
    }
    if (e.childNodes == undefined) return;
    for(var i = 0, c = e.childNodes; i < c.length; i++) {
        zoom_child(c[i], x, zoomed_width_samples);
    }
}
function zoom_parent(e) {
    if (e.attributes) {
        if (e.attributes.x != undefined) {
            e.attributes.x.value = "0.0%";
        }
        if (e.attributes.width != undefined) {
            e.attributes.width.value = "100.0%";
        }
    }
    if (e.childNodes == undefined) return;
    for(var i = 0, c = e.childNodes; i < c.length; i++) {
        zoom_parent(c[i]);
    }
}
function zoom(node) {
    var attr = find_child(node, "rect").attributes;
    var width = parseInt(attr["fg:w"].value);
    var xmin = parseInt(attr["fg:x"].value);
    var xmax = xmin + width;
    var ymin = parseFloat(attr.y.value);
    unzoombtn.classList.remove("hide");
    var el = frames.children;
    var to_update_text = [];
    for (var i = 0; i < el.length; i++) {
        var e = el[i];
        var a = find_child(e, "rect").attributes;
        var ex = parseInt(a["fg:x"].value);
        var ew = parseInt(a["fg:w"].value);
        // Is it an ancestor
        if (!inverted) {
            var upstack = parseFloat(a.y.value) > ymin;
        } else {
            var upstack = parseFloat(a.y.value) < ymin;
        }
        if (upstack) {
            // Direct ancestor
            if (ex <= xmin && (ex+ew) >= xmax) {
                e.classList.add("parent");
                zoom_parent(e);
                to_update_text.push(e);
            }
            // not in current path
            else
                e.classList.add("hide");
        }
        // Children maybe
        else {
            // no common path
            if (ex < xmin || ex >= xmax) {
                e.classList.add("hide");
            }
            else {
                zoom_child(e, xmin, width);
                to_update_text.push(e);
            }
        }
    }
    update_text_for_elements(to_update_text);
}
function unzoom() {
    unzoombtn.classList.add("hide");
    var el = frames.children;
    for(var i = 0; i < el.length; i++) {
        el[i].classList.remove("parent");
        el[i].classList.remove("hide");
        zoom_reset(el[i]);
    }
    update_text_for_elements(el);
}
// search
function reset_search() {
    var el = document.querySelectorAll("#frames rect");
    for (var i = 0; i < el.length; i++) {
        orig_load(el[i], "fill")
    }
    var params = get_params();
    delete params.s;
    history.replaceState(null, null, parse_params(params));
}
function search_prompt() {
    if (!searching) {
        var term = prompt("Enter a search term (regexp " +
            "allowed, eg: ^ext4_)", "");
        if (term != null) {
            search(term)
        }
    } else {
        reset_search();
        searching = 0;
        searchbtn.classList.remove("show");
        searchbtn.firstChild.nodeValue = "Search"
        matchedtxt.classList.add("hide");
        matchedtxt.firstChild.nodeValue = ""
    }
}
function search(term) {
    var re = new RegExp(term);
    var el = frames.children;
    var matches = new Object();
    var maxwidth = 0;
    for (var i = 0; i < el.length; i++) {
        var e = el[i];
        // Skip over frames which are either not visible, or below the zoomed-to frame
        if (e.classList.contains("hide") || e.classList.contains("parent")) {
            continue;
        }
        var func = g_to_func(e);
        var rect = find_child(e, "rect");
        if (func == null || rect == null)
            continue;
        // Save max width. Only works as we have a root frame
        var w = parseInt(rect.attributes["fg:w"].value);
        if (w > maxwidth)
            maxwidth = w;
        if (func.match(re)) {
            // highlight
            var x = parseInt(rect.attributes["fg:x"].value);
            orig_save(rect, "fill");
            rect.attributes.fill.value = searchcolor;
            // remember matches
            if (matches[x] == undefined) {
                matches[x] = w;
            } else {
                if (w > matches[x]) {
                    // overwrite with parent
                    matches[x] = w;
                }
            }
            searching = 1;
        }
    }
    if (!searching)
        return;
    var params = get_params();
    params.s = term;
    history.replaceState(null, null, parse_params(params));

    searchbtn.classList.add("show");
    searchbtn.firstChild.nodeValue = "Reset Search";
    // calculate percent matched, excluding vertical overlap
    var count = 0;
    var lastx = -1;
    var lastw = 0;
    var keys = Array();
    for (k in matches) {
        if (matches.hasOwnProperty(k))
            keys.push(k);
    }
    // sort the matched frames by their x location
    // ascending, then width descending
    keys.sort(function(a, b){
        return a - b;
    });
    // Step through frames saving only the biggest bottom-up frames
    // thanks to the sort order. This relies on the tree property
    // where children are always smaller than their parents.
    for (var k in keys) {
        var x = parseInt(keys[k]);
        var w = matches[keys[k]];
        if (x >= lastx + lastw) {
            count += w;
            lastx = x;
            lastw = w;
        }
    }
    // display matched percent
    matchedtxt.classList.remove("hide");
    var pct = 100 * count / maxwidth;
    if (pct != 100) pct = pct.toFixed(1);
    matchedtxt.firstChild.nodeValue = "Matched: " + pct + "%";
}
function format_percent(n) {
    return n.toFixed(4) + "%";
}
]]></script><rect x="0" y="0" width="100%" height="362" fill="url(#background)"/><text id="title" fill="rgb(0,0,0)" x="50.0000%" y="24.00">py-spy record -o profile.svg --pid 103994 --rate 50 --duration 15</text><text id="details" fill="rgb(0,0,0)" x="10" y="40.00"> </text><text id="unzoom" class="hide" fill="rgb(0,0,0)" x="10" y="24.00">Reset Zoom</text><text id="search" fill="rgb(0,0,0)" x="1190" y="24.00">Search</text><text id="matched" fill="rgb(0,0,0)" x="1190" y="351.00"> </text><svg id="frames" x="10" width="1180" total_samples="4329"><g><title>unrolled_run (trio/_core/_run.py:2765) (5 samples, 0.12%)</title><rect x="0.0000%" y="148" width="0.1155%" height="15" fill="rgb(227,0,7)" fg:x="0" fg:w="5"/><text x="0.2500%" y="158.50"></text></g><g><title>drain (trio/_core/_wakeup_socketpair.py:48) (7 samples, 0.16%)</title><rect x="0.1848%" y="196" width="0.1617%" height="15" fill="rgb(217,0,24)" fg:x="8" fg:w="7"/><text x="0.4348%" y="206.50"></text></g><g><title>task (trio/_core/_entry_queue.py:101) (10 samples, 0.23%)</title><rect x="0.1386%" y="164" width="0.2310%" height="15" fill="rgb(221,193,54)" fg:x="6" fg:w="10"/><text x="0.3886%" y="174.50"></text></g><g><title>wait_woken (trio/_core/_wakeup_socketpair.py:43) (8 samples, 0.18%)</title><rect x="0.1848%" y="180" width="0.1848%" height="15" fill="rgb(248,212,6)" fg:x="8" fg:w="8"/><text x="0.4348%" y="190.50"></text></g><g><title>read (synapsefs/fuse/fs.py:498) (5 samples, 0.12%)</title><rect x="1.0164%" y="180" width="0.1155%" height="15" fill="rgb(208,68,35)" fg:x="44" fg:w="5"/><text x="1.2664%" y="190.50"></text></g><g><title>unrolled_run (trio/_core/_run.py:2866) (51 samples, 1.18%)</title><rect x="0.1386%" y="148" width="1.1781%" height="15" fill="rgb(232,128,0)" fg:x="6" fg:w="51"/><text x="0.3886%" y="158.50"></text></g><g><title>wrapper (_pyfuse3.py:58) (38 samples, 0.88%)</title><rect x="0.4389%" y="164" width="0.8778%" height="15" fill="rgb(207,160,47)" fg:x="19" fg:w="38"/><text x="0.6889%" y="174.50"></text></g><g><title>wait_readable (trio/_core/_generated_io_epoll.py:45) (6 samples, 0.14%)</title><rect x="1.1781%" y="180" width="0.1386%" height="15" fill="rgb(228,23,34)" fg:x="51" fg:w="6"/><text x="1.4281%" y="190.50"></text></g><g><title>wait_readable (trio/_core/_io_epoll.py:334) (6 samples, 0.14%)</title><rect x="1.1781%" y="196" width="0.1386%" height="15" fill="rgb(218,30,26)" fg:x="51" fg:w="6"/><text x="1.4281%" y="206.50"></text></g><g><title>run (trio/_core/_run.py:2532) (61 samples, 1.41%)</title><rect x="0.0000%" y="132" width="1.4091%" height="15" fill="rgb(220,122,19)" fg:x="0" fg:w="61"/><text x="0.2500%" y="142.50"></text></g><g><title>&lt;module&gt; (synapsefs:6) (153 samples, 3.53%)</title><rect x="0.0000%" y="68" width="3.5343%" height="15" fill="rgb(250,228,42)" fg:x="0" fg:w="153"/><text x="0.2500%" y="78.50">&lt;mo..</text></g><g><title>main (synapsefs/cli/main.py:43) (153 samples, 3.53%)</title><rect x="0.0000%" y="84" width="3.5343%" height="15" fill="rgb(240,193,28)" fg:x="0" fg:w="153"/><text x="0.2500%" y="94.50">mai..</text></g><g><title>run (synapsefs/cli/commands/mount.py:66) (153 samples, 3.53%)</title><rect x="0.0000%" y="100" width="3.5343%" height="15" fill="rgb(216,20,37)" fg:x="0" fg:w="153"/><text x="0.2500%" y="110.50">run..</text></g><g><title>mount_fuse (synapsefs/fuse/daemon.py:194) (153 samples, 3.53%)</title><rect x="0.0000%" y="116" width="3.5343%" height="15" fill="rgb(206,188,39)" fg:x="0" fg:w="153"/><text x="0.2500%" y="126.50">mou..</text></g><g><title>run (trio/_core/_run.py:2535) (92 samples, 2.13%)</title><rect x="1.4091%" y="132" width="2.1252%" height="15" fill="rgb(217,207,13)" fg:x="61" fg:w="92"/><text x="1.6591%" y="142.50">r..</text></g><g><title>get_events (trio/_core/_io_epoll.py:243) (92 samples, 2.13%)</title><rect x="1.4091%" y="148" width="2.1252%" height="15" fill="rgb(231,73,38)" fg:x="61" fg:w="92"/><text x="1.6591%" y="158.50">g..</text></g><g><title>_work (trio/_core/_thread_cache.py:211) (170 samples, 3.93%)</title><rect x="3.5343%" y="116" width="3.9270%" height="15" fill="rgb(225,20,46)" fg:x="153" fg:w="170"/><text x="3.7843%" y="126.50">_wor..</text></g><g><title>read_bytes (pathlib/__init__.py:782) (24 samples, 0.55%)</title><rect x="10.3488%" y="276" width="0.5544%" height="15" fill="rgb(210,31,41)" fg:x="448" fg:w="24"/><text x="10.5988%" y="286.50"></text></g><g><title>open (pathlib/__init__.py:776) (21 samples, 0.49%)</title><rect x="10.4181%" y="292" width="0.4851%" height="15" fill="rgb(221,200,47)" fg:x="451" fg:w="21"/><text x="10.6681%" y="302.50"></text></g><g><title>_rows_from_manifest (synapsefs/graph.py:706) (211 samples, 4.87%)</title><rect x="10.3026%" y="244" width="4.8741%" height="15" fill="rgb(226,26,5)" fg:x="446" fg:w="211"/><text x="10.5526%" y="254.50">_rows_..</text></g><g><title>get (synapsefs/store/objectstore.py:119) (209 samples, 4.83%)</title><rect x="10.3488%" y="260" width="4.8279%" height="15" fill="rgb(249,33,26)" fg:x="448" fg:w="209"/><text x="10.5988%" y="270.50">get (s..</text></g><g><title>read_bytes (pathlib/__init__.py:783) (185 samples, 4.27%)</title><rect x="10.9032%" y="276" width="4.2735%" height="15" fill="rgb(235,183,28)" fg:x="472" fg:w="185"/><text x="11.1532%" y="286.50">read_..</text></g><g><title>read_bytes (pathlib/__init__.py:782) (39 samples, 0.90%)</title><rect x="15.2229%" y="308" width="0.9009%" height="15" fill="rgb(221,5,38)" fg:x="659" fg:w="39"/><text x="15.4729%" y="318.50"></text></g><g><title>open (pathlib/__init__.py:776) (28 samples, 0.65%)</title><rect x="15.4770%" y="324" width="0.6468%" height="15" fill="rgb(247,18,42)" fg:x="670" fg:w="28"/><text x="15.7270%" y="334.50"></text></g><g><title>_rows_from_manifest (synapsefs/graph.py:706) (318 samples, 7.35%)</title><rect x="15.2229%" y="276" width="7.3458%" height="15" fill="rgb(241,131,45)" fg:x="659" fg:w="318"/><text x="15.4729%" y="286.50">_rows_from..</text></g><g><title>get (synapsefs/store/objectstore.py:119) (318 samples, 7.35%)</title><rect x="15.2229%" y="292" width="7.3458%" height="15" fill="rgb(249,31,29)" fg:x="659" fg:w="318"/><text x="15.4729%" y="302.50">get (synap..</text></g><g><title>read_bytes (pathlib/__init__.py:783) (279 samples, 6.44%)</title><rect x="16.1238%" y="308" width="6.4449%" height="15" fill="rgb(225,111,53)" fg:x="698" fg:w="279"/><text x="16.3738%" y="318.50">read_byt..</text></g><g><title>decode_chunk (synapsefs/codec/chunk.py:641) (769 samples, 17.76%)</title><rect x="22.5687%" y="292" width="17.7639%" height="15" fill="rgb(238,160,17)" fg:x="977" fg:w="769"/><text x="22.8187%" y="302.50">decode_chunk (synapsefs/code..</text></g><g><title>plain_stream (synapsefs/codec/chunk.py:606) (767 samples, 17.72%)</title><rect x="22.6149%" y="308" width="17.7177%" height="15" fill="rgb(214,148,48)" fg:x="979" fg:w="767"/><text x="22.8649%" y="318.50">plain_stream (synapsefs/code..</text></g><g><title>unshuffle_to_array (synapsefs/codec/chunk.py:357) (6 samples, 0.14%)</title><rect x="40.3557%" y="308" width="0.1386%" height="15" fill="rgb(232,36,49)" fg:x="1747" fg:w="6"/><text x="40.6057%" y="318.50"></text></g><g><title>decode_chunk (synapsefs/codec/chunk.py:644) (746 samples, 17.23%)</title><rect x="40.3326%" y="292" width="17.2326%" height="15" fill="rgb(209,103,24)" fg:x="1746" fg:w="746"/><text x="40.5826%" y="302.50">decode_chunk (synapsefs/cod..</text></g><g><title>unshuffle_to_array (synapsefs/codec/chunk.py:360) (739 samples, 17.07%)</title><rect x="40.4943%" y="308" width="17.0709%" height="15" fill="rgb(229,88,8)" fg:x="1753" fg:w="739"/><text x="40.7443%" y="318.50">unshuffle_to_array (synaps..</text></g><g><title>_rows_from_manifest (synapsefs/graph.py:722) (1,516 samples, 35.02%)</title><rect x="22.5687%" y="276" width="35.0196%" height="15" fill="rgb(213,181,19)" fg:x="977" fg:w="1516"/><text x="22.8187%" y="286.50">_rows_from_manifest (synapsefs/graph.py:722)</text></g><g><title>_rows_from_manifest (synapsefs/graph.py:731) (103 samples, 2.38%)</title><rect x="57.5884%" y="276" width="2.3793%" height="15" fill="rgb(254,191,54)" fg:x="2493" fg:w="103"/><text x="57.8384%" y="286.50">_r..</text></g><g><title>_aligned_base (synapsefs/graph.py:665) (1,939 samples, 44.79%)</title><rect x="15.1998%" y="260" width="44.7909%" height="15" fill="rgb(241,83,37)" fg:x="658" fg:w="1939"/><text x="15.4498%" y="270.50">_aligned_base (synapsefs/graph.py:665)</text></g><g><title>_rows_from_manifest (synapsefs/graph.py:720) (1,941 samples, 44.84%)</title><rect x="15.1767%" y="244" width="44.8371%" height="15" fill="rgb(233,36,39)" fg:x="657" fg:w="1941"/><text x="15.4267%" y="254.50">_rows_from_manifest (synapsefs/graph.py:720)</text></g><g><title>decode_chunk (synapsefs/codec/chunk.py:641) (433 samples, 10.00%)</title><rect x="60.0370%" y="260" width="10.0023%" height="15" fill="rgb(226,3,54)" fg:x="2599" fg:w="433"/><text x="60.2870%" y="270.50">decode_chunk (..</text></g><g><title>plain_stream (synapsefs/codec/chunk.py:606) (431 samples, 9.96%)</title><rect x="60.0832%" y="276" width="9.9561%" height="15" fill="rgb(245,192,40)" fg:x="2601" fg:w="431"/><text x="60.3332%" y="286.50">plain_stream (..</text></g><g><title>decode_chunk (synapsefs/codec/chunk.py:644) (674 samples, 15.57%)</title><rect x="70.0393%" y="260" width="15.5694%" height="15" fill="rgb(238,167,29)" fg:x="3032" fg:w="674"/><text x="70.2893%" y="270.50">decode_chunk (synapsefs/..</text></g><g><title>unshuffle_to_array (synapsefs/codec/chunk.py:360) (670 samples, 15.48%)</title><rect x="70.1317%" y="276" width="15.4770%" height="15" fill="rgb(232,182,51)" fg:x="3036" fg:w="670"/><text x="70.3817%" y="286.50">unshuffle_to_array (syna..</text></g><g><title>_rows_from_manifest (synapsefs/graph.py:722) (1,542 samples, 35.62%)</title><rect x="60.0139%" y="244" width="35.6202%" height="15" fill="rgb(231,60,39)" fg:x="2598" fg:w="1542"/><text x="60.2639%" y="254.50">_rows_from_manifest (synapsefs/graph.py:722)</text></g><g><title>decode_chunk (synapsefs/codec/chunk.py:671) (433 samples, 10.00%)</title><rect x="85.6318%" y="260" width="10.0023%" height="15" fill="rgb(208,69,12)" fg:x="3707" fg:w="433"/><text x="85.8818%" y="270.50">decode_chunk (..</text></g><g><title>rows (synapsefs/graph.py:497) (3,819 samples, 88.22%)</title><rect x="10.2795%" y="228" width="88.2190%" height="15" fill="rgb(235,93,37)" fg:x="445" fg:w="3819"/><text x="10.5295%" y="238.50">rows (synapsefs/graph.py:497)</text></g><g><title>_rows_from_manifest (synapsefs/graph.py:731) (124 samples, 2.86%)</title><rect x="95.6341%" y="244" width="2.8644%" height="15" fill="rgb(213,116,39)" fg:x="4140" fg:w="124"/><text x="95.8841%" y="254.50">_r..</text></g><g><title>_chunk_rows (synapsefs/fuse/reconstruct.py:168) (3,820 samples, 88.24%)</title><rect x="10.2795%" y="212" width="88.2421%" height="15" fill="rgb(222,207,29)" fg:x="445" fg:w="3820"/><text x="10.5295%" y="222.50">_chunk_rows (synapsefs/fuse/reconstruct.py:168)</text></g><g><title>_get_rows (synapsefs/fuse/reconstruct.py:192) (3,822 samples, 88.29%)</title><rect x="10.2795%" y="196" width="88.2883%" height="15" fill="rgb(206,96,30)" fg:x="445" fg:w="3822"/><text x="10.5295%" y="206.50">_get_rows (synapsefs/fuse/reconstruct.py:192)</text></g><g><title>read (synapsefs/fuse/reconstruct.py:142) (3,829 samples, 88.45%)</title><rect x="10.2564%" y="180" width="88.4500%" height="15" fill="rgb(218,138,4)" fg:x="444" fg:w="3829"/><text x="10.5064%" y="190.50">read (synapsefs/fuse/reconstruct.py:142)</text></g><g><title>_get_rows (synapsefs/fuse/reconstruct.py:193) (6 samples, 0.14%)</title><rect x="98.5678%" y="196" width="0.1386%" height="15" fill="rgb(250,191,14)" fg:x="4267" fg:w="6"/><text x="98.8178%" y="206.50"></text></g><g><title>worker_fn (trio/_threads.py:392) (3,950 samples, 91.25%)</title><rect x="7.5999%" y="164" width="91.2451%" height="15" fill="rgb(239,60,40)" fg:x="329" fg:w="3950"/><text x="7.8499%" y="174.50">worker_fn (trio/_threads.py:392)</text></g><g><title>_handle_job (trio/_core/_thread_cache.py:183) (3,959 samples, 91.45%)</title><rect x="7.4613%" y="132" width="91.4530%" height="15" fill="rgb(206,27,48)" fg:x="323" fg:w="3959"/><text x="7.7113%" y="142.50">_handle_job (trio/_core/_thread_cache.py:183)</text></g><g><title>capture (outcome/_impl.py:67) (3,959 samples, 91.45%)</title><rect x="7.4613%" y="148" width="91.4530%" height="15" fill="rgb(225,35,8)" fg:x="323" fg:w="3959"/><text x="7.7113%" y="158.50">capture (outcome/_impl.py:67)</text></g><g><title>_handle_job (trio/_core/_thread_cache.py:189) (6 samples, 0.14%)</title><rect x="98.9605%" y="132" width="0.1386%" height="15" fill="rgb(250,213,24)" fg:x="4284" fg:w="6"/><text x="99.2105%" y="142.50"></text></g><g><title>namefunc (trio/_core/_thread_cache.py:38) (6 samples, 0.14%)</title><rect x="98.9605%" y="148" width="0.1386%" height="15" fill="rgb(247,123,22)" fg:x="4284" fg:w="6"/><text x="99.2105%" y="158.50"></text></g><g><title>run_sync_soon (trio/_core/_entry_queue.py:136) (6 samples, 0.14%)</title><rect x="99.1915%" y="180" width="0.1386%" height="15" fill="rgb(231,138,38)" fg:x="4294" fg:w="6"/><text x="99.4415%" y="190.50"></text></g><g><title>all (4,329 samples, 100%)</title><rect x="0.0000%" y="52" width="100.0000%" height="15" fill="rgb(231,145,46)" fg:x="0" fg:w="4329"/><text x="0.2500%" y="62.50"></text></g><g><title>_bootstrap (threading.py:1043) (4,176 samples, 96.47%)</title><rect x="3.5343%" y="68" width="96.4657%" height="15" fill="rgb(251,118,11)" fg:x="153" fg:w="4176"/><text x="3.7843%" y="78.50">_bootstrap (threading.py:1043)</text></g><g><title>_bootstrap_inner (threading.py:1081) (4,176 samples, 96.47%)</title><rect x="3.5343%" y="84" width="96.4657%" height="15" fill="rgb(217,147,25)" fg:x="153" fg:w="4176"/><text x="3.7843%" y="94.50">_bootstrap_inner (threading.py:1081)</text></g><g><title>run (threading.py:1023) (4,176 samples, 96.47%)</title><rect x="3.5343%" y="100" width="96.4657%" height="15" fill="rgb(247,81,37)" fg:x="153" fg:w="4176"/><text x="3.7843%" y="110.50">run (threading.py:1023)</text></g><g><title>_work (trio/_core/_thread_cache.py:213) (4,006 samples, 92.54%)</title><rect x="7.4613%" y="116" width="92.5387%" height="15" fill="rgb(209,12,38)" fg:x="323" fg:w="4006"/><text x="7.7113%" y="126.50">_work (trio/_core/_thread_cache.py:213)</text></g><g><title>_handle_job (trio/_core/_thread_cache.py:204) (39 samples, 0.90%)</title><rect x="99.0991%" y="132" width="0.9009%" height="15" fill="rgb(227,1,9)" fg:x="4290" fg:w="39"/><text x="99.3491%" y="142.50"></text></g><g><title>deliver_worker_fn_result (trio/_threads.py:421) (37 samples, 0.85%)</title><rect x="99.1453%" y="148" width="0.8547%" height="15" fill="rgb(248,47,43)" fg:x="4292" fg:w="37"/><text x="99.3953%" y="158.50"></text></g><g><title>run_sync_soon (trio/_core/_entry_queue.py:223) (37 samples, 0.85%)</title><rect x="99.1453%" y="164" width="0.8547%" height="15" fill="rgb(221,10,30)" fg:x="4292" fg:w="37"/><text x="99.3953%" y="174.50"></text></g><g><title>run_sync_soon (trio/_core/_entry_queue.py:148) (28 samples, 0.65%)</title><rect x="99.3532%" y="180" width="0.6468%" height="15" fill="rgb(210,229,1)" fg:x="4301" fg:w="28"/><text x="99.6032%" y="190.50"></text></g><g><title>wakeup_thread_and_signal_safe (trio/_core/_wakeup_socketpair.py:39) (27 samples, 0.62%)</title><rect x="99.3763%" y="196" width="0.6237%" height="15" fill="rgb(222,148,37)" fg:x="4302" fg:w="27"/><text x="99.6263%" y="206.50"></text></g></svg></svg>